apiVersion: v1
kind: Pod
metadata:
  name: security-nonroot
spec:
  securityContext:
    runAsUser: 1000
    runAsGroup: 1000
    runAsNonRoot: true

  containers:
    - name: test
      image: busybox
      command: ["sh", "-c", "id; sleep 3600"]
